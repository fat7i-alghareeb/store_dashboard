/// Keys used by [StorageService] for localization-related values.
class LocalizationStorageKeys {
  LocalizationStorageKeys._();

  /// Stores the current language code (e.g. 'en', 'ar').
  static const String localeCode = 'locale_code';
}
